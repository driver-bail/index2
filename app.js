// Prompt the user for their name
var userName = prompt("What's your name?");

// Display a greeting
if (userName) {
    alert("Hello, " + userName + "! Welcome to the Greeting App.");
} else {
    alert("Hello, Guest! Welcome to the Greeting App.");
}
